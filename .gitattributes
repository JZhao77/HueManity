# Auto-detect text files and normalize them
* text=auto

# Normalize common code files (leave CRLF on Windows, LF in repo)
*.ts
*.tsx
*.js
*.jsx
*.css
*.html

# Force certain file types to LF (good for scripts/JSON)
*.sh eol=lf
*.json eol=lf
